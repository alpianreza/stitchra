<?php
use Illuminate\Database\Migrations\Migration;use Illuminate\Database\Schema\Blueprint;use Illuminate\Support\Facades\DB;use Illuminate\Support\Facades\Schema;
return new class extends Migration{public function up():void{Schema::create('period_close_runs',function(Blueprint$t){$t->id();$t->foreignId('company_id')->constrained('companies')->restrictOnDelete();$t->string('period',7);$t->string('status',16)->default('DRAFT');$t->json('checklist_snapshot');$t->json('attestations');$t->char('snapshot_hash',64);$t->unsignedBigInteger('prepared_by');$t->timestamp('prepared_at',6);$t->unsignedBigInteger('approved_by')->nullable();$t->timestamp('approved_at',6)->nullable();$t->unsignedBigInteger('closed_by')->nullable();$t->timestamp('closed_at',6)->nullable();$t->timestamps(6);$t->unique(['company_id','period'],'uq_period_close_run');});DB::statement("ALTER TABLE period_close_runs ADD CONSTRAINT chk_period_close_status CHECK (status IN ('DRAFT','READY','APPROVED','CLOSED'))");}public function down():void{Schema::dropIfExists('period_close_runs');}};
